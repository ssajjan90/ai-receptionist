<configuration>

  <springProfile name="default,local,test">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] [tenantId=%X{tenantId}] %-5level %logger{36} - %msg%n</pattern>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
    <logger name="com.aireceptionist" level="DEBUG"/>
  </springProfile>

  <springProfile name="prod">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <!-- Structured key=value format readable by Grafana Cloud log parser -->
        <pattern>ts=%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ} level=%-5level thread=%thread tenantId=%X{tenantId} logger=%logger{36} msg="%msg"%n</pattern>
      </encoder>
    </appender>
    <root level="WARN">
      <appender-ref ref="CONSOLE"/>
    </root>
    <logger name="com.aireceptionist" level="INFO"/>
  </springProfile>

</configuration>
